#!/bin/sh

## LLTD monitor
	local lltd_pid
	system_date=$(date)
	# check lltd daemon			
	lltd_pid=$(ps | grep "/sbin/lltd.sh" | grep "grep" -v | awk '{print $1}')
		
	if [ "$lltd_pid" == "" ] ;then
		#echo "$system_date  lltd_die" >> /tmp/lltd_monitor_log			
		/sbin/lltd.sh &		
	fi	
