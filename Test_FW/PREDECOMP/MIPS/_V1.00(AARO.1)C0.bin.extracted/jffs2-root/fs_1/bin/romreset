#!/bin/sh

if [ "$1" -eq "0" ]; then
        firstboot
        echo "No reboot"
elif [ "$1" -eq "1" ]; then
        firstboot
	sleep 3
	reboot
        echo "Rebooting..."
else
        echo "Wrong parameter"
fi
